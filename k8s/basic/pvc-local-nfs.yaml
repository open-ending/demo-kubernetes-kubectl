apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-local-nfs
  namespace: u1
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
#  selector:
#    matchLabels:
#      type: nfs